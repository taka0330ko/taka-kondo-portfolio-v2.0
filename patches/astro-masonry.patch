diff --git a/src/components/Masonry.astro b/src/components/Masonry.astro
index 744ce47b4aec8e26ab1e591cdeff436bbada4e3f..cfba793e38c6a0870cac663c1dd40a190f66b80c 100644
--- a/src/components/Masonry.astro
+++ b/src/components/Masonry.astro
@@ -66,11 +66,11 @@ const breakpointColsString =
   let initialized = false;
 
   const initMasonry = () => {
-    console.log("Initializing masonry.");
-
     const masonryContainer = document.querySelector("[data-masonry-container]");
     if (!masonryContainer) {
-      console.warn("Masonry container not found.");
+      // No masonry container on this page (e.g. after navigating away from
+      // the Archive page under Astro's persistent View Transitions router).
+      // This is expected, not an error, so stay silent instead of warning.
       return;
     }
 
